name: fvast_admin
description: A new Flutter application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^1.1.2
  flushbar: ^1.10.4
  flutter_polyline_points: ^0.1.0
  google_maps_flutter:   # TODO configure for iOS
  firebase_core: ^0.5.1
  firebase_auth: ^0.18.1+2
  cloud_firestore:
  intl:
  flutter_svg: ^0.18.1
  url_launcher:
  flutter_spinkit: ^4.1.2
  cached_network_image:
  logger: ^0.9.4
  loading_overlay: ^0.2.1
  flutter_staggered_grid_view: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:

  uses-material-design: true


  assets:
    - images/
